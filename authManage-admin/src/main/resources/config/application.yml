server:
  port: 8001


spring:
  profiles:
    active: dev1
  jackson:
    time-zone: GMT+8

#jwt
jwt:
  header: Authorization
  secret: mySecret
  # token 过期时间 2个小时
  expiration: 7200000
  auth:
    # 授权路径
    path: /login
    # 获取用户信息
    account: /info


